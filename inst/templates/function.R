#' {func_name}
#'
#' Equivalent to {module_name}${pyfunc}
#' See `reticulate::py_help({module_name}${pyfunc})` for help
#' @md
#' {if (params != "") "@param" else ""} {params} {if (params != "") glue("Parameters for class constructor function. See `reticulate::py_help({module_name}${pyfunc})` for help") else ""}
#' 
#' @export
